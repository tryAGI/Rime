# Rime.CLI

Command-line interface for the Rime SDK generated with AutoSDK.

## Installation

```bash
dotnet tool install --global Rime.CLI --prerelease
```

## Usage

```bash
rime --help
rime api --help
```